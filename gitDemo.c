#include <stdio.h>
#define task "push and pull"
int main(){
 char title[100]="command key words[1]";
 char str[200]="git init, git status, git add, git commit, git log";
 printf("\t%s\n\n %s\n", title, str);
    
    return 0;

}
