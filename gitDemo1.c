#include <stdio.h>

int main(){
	char title[100]="git key words[2]";
	char keyWords[200]="commit -m 'message', commit -am 'message'";
	char branch[200]="git branch exp, git checkout exp, git branch, ls -al, git branch, git log --branches --decorate, git log --branches --decorate --graph, git log master .. esp";
	
printf("\t%s\n\n %s\n\n %s\n",  title, keyWords, branch);


    
    return 0;

}
